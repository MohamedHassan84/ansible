# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  say-hello:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: amazon/aws-cli
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - run:
          name: install tar gzip
          command: |
            yum update
            yum -y install tar gzip

      - run: mkdir -p ~/workspace/.circleci/ansible/

      - run:
          name: Add back-end ip to ansible inventory
          command: |
            cd  ~/workspace/.circleci/ansible/
            echo "[web1]"> ~/workspace/.circleci/ansible/inventory.txt
            cat inventory.txt
            pwd
      
      - persist_to_workspace:
          root: ~/
          paths:
            - ~/workspace/*
  configure-infrastructure:
    #working_directory: /app
    docker:
      - image: circleci/python:3.6.2
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            sudo apt update
            # # sudo python3 -m pip install -U pip
            # #sudo apt update
            # #sudo apt upgrade
            # # pip install --user ansible
            # sudo apt install software-properties-common --force-yes
            # sudo add-apt-repository --yes ppa:ansible/ansible 
            # # sudo apt update
            # sudo apt install ansible --yes
            sudo apt install tar gzip
            sudo apt install -yy less
            # curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            # sudo unzip awscliv2.zip
            # sudo ./aws/install
            #sudo systemd daemon-reload 
            pwd
      - attach_workspace:
         at: ~/
      - run:
          name: Configure server
          command: |
            #cd /root/project/.circleci/ansible/
            pwd
            ls ~/
            ls ~/project

            #cat ~/workspace/.circleci/ansible/inventory.txt
            #ansible-playbook -i "~/app/.circleci/ansible/inventory.txt" configure-server.yml

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
      - configure-infrastructure